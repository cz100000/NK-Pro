# AP22F7B Korrektur 1 – Schutz-Hash-Bericht

## Ergebnis

Bestanden.

- 435 vollständig geschützte Dateien: unverändert;
- 13 geschützte Teilbereiche: unverändert;
- geschützter CSS-Präfix mit 304.202 Byte: unverändert; die Korrektur wurde ausschließlich als neuer `#mieter`-Block angehängt;
- geschützte Bereiche vor und nach `renderWohnungen()` in `js/ui-costs.js`: unverändert;
- Navigation, Datenmodell, Persistenz, Migration, Archivierung, Berechnung, Qualität, Brief und Druck: nicht geändert.

Der maschinelle Nachweis erfolgt durch `tests/ap22f7b-protected-areas.test.cjs` und ist bestanden.
